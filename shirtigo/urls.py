from django.urls import path
from . import views

urlpatterns = [
    # Aggiungerai URL qui quando crei le views
    # Esempio:
    # path('order/<int:order_id>/', views.order_status, name='order_status'),
]